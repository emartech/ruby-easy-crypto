sudo: false
language: ruby
rvm:
- 2.4.1
before_install: gem install bundler -v 1.16.1
deploy:
  provider: rubygems
  api_key: ${RUBYGEMS_API_KEY}
  gemspec: easy-crypto.gemspec
  skip_cleanup: true
  on:
    tags: true
